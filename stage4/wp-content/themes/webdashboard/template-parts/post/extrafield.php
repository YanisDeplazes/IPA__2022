<?php 
       
   /**
   * Grid Template for Extra fields on any Single Page
   *
   * @link https://developer.wordpress.org/themes/basics/template-hierarchy/
   */

 /*   if( have_rows('extra_fields'){
        while( have_rows('extra_fields') ) : the_row();

        $title = get_sub_field('title');
        get_template_part( 'template-parts/components/box/box', 'box', array('key'   => 'start','hasPadding' => true)); 
        $content = '<div class="loop-wrapper"><table class="loop simple">';
        $table_view = get_sub_field('table_view');
        while( have_rows('table_view') ) : the_row();
            echo'<tr><th class="overline">'. get_sub_field('title') .'</th><?php }?><td class="body-2 right-text">'.  get_sub_field('content')  .'</td></tr>'; /* Output of Table Content
        endwhile;
        $content .= '</table></div>';
        get_template_part( 'template-parts/post/content', 'content', array('content' => $content,'title' => $title)); 
        get_template_part( 'template-parts/components/box/box', 'box', array('key'   => 'end','hasPadding' => true));  
    endwhile;

    }

*/

        
?>